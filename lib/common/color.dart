import 'package:flutter/material.dart';

class ColorStyles{
  // 기본 색상
  static const Color black = Color(0xFF000000);                        // 기본 블랙
  static const Color white = Color(0xFFFFFFFF);                        // 기본 화이트
  static const Color navy = Color(0xFF001F3F);                         // 네이비
  static const Color blue = Color(0xFF007BFF);                         // 블루
  static const Color gray = Color(0xFFB0B0B0);                         // 회색
  static const Color whiteGray = Color.fromARGB(255, 202, 201, 201);   // 밝은 회색
  static const Color darkgray = Color.fromARGB(255, 54, 51, 51);       // 어두운 회색
  static const Color orange = Color(0xFFFFA500);                       // 주황
  static const Color green = Color(0xFFB3E5FC);                        // 연초록
  static const Color yellow = Color(0xFFFFEB3B);                       // 노랑
  static const Color red = Color(0xFFE62E2E);                          // 빨강
  static const Color transparent = Colors.transparent;                       // 무색
  
}